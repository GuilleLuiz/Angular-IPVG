import { SiempreMinusculaDirective } from './siempre-minuscula.directive';

describe('SiempreMinusculaDirective', () => {
  it('should create an instance', () => {
    const directive = new SiempreMinusculaDirective();
    expect(directive).toBeTruthy();
  });
});
